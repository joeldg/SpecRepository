{
  "name": "specregistry",
  "version": "0.1.0",
  "private": true,
  "description": "SpecRegistry — centralized management, versioning, and distribution of Markdown project specifications",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w @specregistry/shared && npm run build -w @specregistry/server -w @specregistry/cli -w @specregistry/mcp -w @specregistry/web",
    "test": "npm run test --workspaces --if-present",
    "dev:server": "npm run dev -w @specregistry/server",
    "dev:web": "npm run dev -w @specregistry/web",
    "seed": "npm run seed -w @specregistry/server"
  },
  "engines": {
    "node": ">=20"
  }
}
