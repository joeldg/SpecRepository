apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "specregistry.fullname" . }}-env
  labels:
    {{- include "specregistry.labels" . | nindent 4 }}
data:
  {{- range $key, $val := .Values.env }}
  {{ $key }}: {{ $val | quote }}
  {{- end }}
